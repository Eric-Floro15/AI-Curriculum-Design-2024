# Core agent framework
crewai>=0.80.0
crewai-tools>=0.14.0

# LLM providers (install all; active provider set via LLM_PROVIDER in .env)
openai>=1.30.0
anthropic>=0.28.0

# RAG / vector store
faiss-cpu>=1.8.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0

# Embeddings (local fallback option)
sentence-transformers>=3.0.0

# Data loading
pandas>=2.0.0
openpyxl>=3.1.0

# Web search (University Programs agent)
duckduckgo-search>=6.0.0

# RSS feeds (News agent)
feedparser>=6.0.0

# Chat UI
chainlit>=1.1.0

# Utilities
python-dotenv>=1.0.0
